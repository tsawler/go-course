{{template "base" .}}

{{define "content"}}

    <div class="container">
        <div class="row">
            <div class="col">
                <p class="mt-4">This is a the about page, served from a Go template.</p>
            </div>
        </div>
    </div>

{{end}}